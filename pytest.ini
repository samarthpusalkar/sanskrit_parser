[pytest]
markers =
    benchmark: canonical sutra and fixture catalog validation
    integrity: dynamic execution and hardcoding-detection checks
    adapter: abstract adapter contract checks
    regression: frozen benchmark summary regression checks
